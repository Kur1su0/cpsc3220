
CC=clang
CFLAGS=-Wall -g

BINS=shimtest shim.so


all: $(BINS)

shim.so:  shim.c
	$(CC) $(CFLAGS) -fPIC -shared -o shim.so shim.c -ldl

shimtest: shimtest.c
	$(CC) $(CFLAGS) -o shimtest shimtest.c

clean:
	rm $(BINS)
